class Solution:
  def twoSum(self, nums: [int], target: int) -> [int]:
    print(nums)
    
    for i in range(len(nums)):
      print(nums[i])
      for n in numbs[i + 1:]:
        if ((nums[i] + ))
      
      # if (target - n):
        
    # return [1]
  
sol = Solution()
sol.twoSum([1,2], 2)